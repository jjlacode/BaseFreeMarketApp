<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ProgressBarStyleAcept">
        <item name="colorAccent">@color/Color_contador_acept</item>
    </style>
    <style name="ProgressBarStyleOk">
        <item name="colorAccent">@color/Color_contador_ok</item>
    </style>
    <style name="ProgressBarStyleNotok">
        <item name="colorAccent">@color/Color_contador_notok</item>
    </style>

</resources>
